namespace Crumpet.Console;

public static class Program
{
    public static void Main(string[] args)
    {
        // IInterpreter interpreter = new Parser.CrumpetInterpreter();
        // interpreter.Execute(File.ReadAllText(args[1]));
    }
}