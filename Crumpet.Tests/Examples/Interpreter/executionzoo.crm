func void main(string[] args){
    int i;
    i = 0 + 1 + 2;
    i = 0;
    while(i < count(args)){
        println(args[i]);
        
        i = i + 1;
    }
    
    string inVal;
    inVal = input();
    println(input());
    int index;
    index = pInt(inVal);
    println("Outputting value at index " + pString(index) + " " + args[index]);
}