struct testType{
    int one;
    bool two;
    string three;
    float four;
}

func int main(){
    testType testInstance;
    foo(testInstance);
}

func testType foo(testType bar){
    int test;
    test = bar.one = 2;
    bar.one = 1;
    bar.two = false;
    bar.three = "test";
    bar.four = 4.0;
}

func void foo2(testType bar, int testVal)
{
    // comment
    bar.one = (testVal * testVal) / 2 - (1 + 3);
    bar.two = one >= 2;
    
    int i;
    i = 0;
    while(i < testVal){
        i = i + 1;
    } 
    
    return;
}